# Selected by `npm run dev:mock` / `npm run build:mock` (vite --mode mock).
#
# Mock mode runs the whole GUI from data generated in the browser: no ROS 2, no
# Python backend, no Jetson, no boat. It is permanent, not a throwaway — it is
# how the interface gets developed and reviewed without taking the vessel out.
VITE_MOCK=true
