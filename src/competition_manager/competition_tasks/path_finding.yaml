# TODO: no course waypoints configured yet. competition_manager rejects
# /competition/start for this task while the list is empty (path_finding is
# a waypoint_required_task) — this is intentionally fail-closed until the
# real path-finding-leg waypoints are added.
version: 1

task:
  id: path_finding
  name: Path Finding
  description: Autonomous path-finding competition task.

mission:
  waypoints: []
